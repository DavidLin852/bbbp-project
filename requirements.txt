# ============================================================
# BBB Permeability Prediction Platform - Python Dependencies
# ============================================================
#
# RECOMMENDED: Use split dependency files for cleaner setup
# ============================================================
#
# For baseline pipeline (RF, XGB, LGBM models):
#   pip install -r requirements-baseline.txt
#   conda env create -f configs/environment-baseline.yml
#
# For research modules (VAE, GAN, GNN, Streamlit):
#   pip install -r requirements-baseline.txt
#   pip install -r requirements-research.txt
#   conda env create -f configs/environment-research.yml
#
# ============================================================
# LEGACY: All dependencies in one file (backward compatibility)
# ============================================================
#
# If you prefer to install everything at once, you can use:
#   pip install -r requirements-legacy.txt
#
# Note: This includes deep learning frameworks (PyTorch, PyG) which
# are large and not needed for the baseline pipeline.
#
# ============================================================
# QUICK START
# ============================================================
#
# For baseline work (RECOMMENDED):
#   1. conda env create -f configs/environment-baseline.yml
#   2. conda activate bbb-baseline
#   3. python scripts/baseline/01_preprocess_b3db.py --seed 0
#
# For research work:
#   1. conda env create -f configs/environment-research.yml
#   2. conda activate bbb-research
#   3. python src/vae/train_vae.py  # (example)
#
# ============================================================
# DEPENDENCY FILES
# ============================================================
#
# requirements-baseline.txt       - Core baseline dependencies only
# requirements-research.txt       - Research modules (VAE, GAN, GNN, etc.)
# requirements-legacy.txt         - All dependencies (backward compatibility)
#
# configs/environment-baseline.yml    - Conda environment for baseline
# configs/environment-research.yml    - Conda environment for research
# configs/environment.yml              - Original conda environment (all deps)
#
# ============================================================
